image: fedora:29
build:
  coverage: '/^\s*Total:\|(\d+\.\d+\%)\s+.*$/'
  script:
    - dnf install -y lcov gcc openssl-devel make clang
    - make -j world
    - make -j analyze
    - make run-lcov || ":"
    - lcov --list dhcpd-pd.lcov.info || ":"
